# Climb On Documentation

Start with [EMPLOYEE_HANDOFF.md](EMPLOYEE_HANDOFF.md). It explains the repository,
local setup, GitHub workflow, Supabase setup order, verification, and deployment.

Older phase notes are preserved in [phases/](phases/) for historical context. They
are not the source of truth for current setup; use the employee handoff and the
README files in the repository root and `supabase/` instead.
