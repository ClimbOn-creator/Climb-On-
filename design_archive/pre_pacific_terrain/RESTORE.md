# Pre-Pacific Terrain snapshot

This folder is a complete copy of the Climb On codebase at commit `dc72040`,
immediately before the Pacific Terrain redesign was introduced.

It is intentionally not connected to the running application. To restore an
individual file, copy its archived version back to the matching path at the
repository root. The Git commit remains the safest way to restore the entire
app at once.
