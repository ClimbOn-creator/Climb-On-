# Archived Instagram-style feed

`feed_screen.dart.disabled` is the complete Feed screen that was replaced by
the utility-first Field Guide. The `.disabled` extension keeps it out of the
Flutter build while preserving the old implementation for reference or
restoration.
